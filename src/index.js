'use strict';

import React from 'react';
import ReactDOM from 'react-dom';
import AppWrapper from './components/appWrapper/appWrapper';

ReactDOM.render(React.createElement(AppWrapper), document.getElementById('app-wrapper'));
